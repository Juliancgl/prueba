# prueba
-install or execute the project in iss 
-it must execute the script of db 
-change the conexionstring in web.config and app.config in the case of test project
-in app insert automatic  the user 'prueba' and password='12345678'
